import styled from "styled-components";
import { colors, spacing } from "../../../global/vars";


const SHr = styled.hr`
  border-color: ${colors.primary200} 0;
  margin: ${spacing.space4};
`;

export default SHr;